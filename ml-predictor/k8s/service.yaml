apiVersion: v1
kind: Service
metadata:
  name: ml-predictor
  namespace: monitoring
  labels:
    app: ml-predictor
spec:
  selector:
    app: ml-predictor
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP
  type: ClusterIP
