apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-main
  namespace: myapp
spec:
  ingressClassName: traefik
  rules:
  - host: web.mycompany.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-service
            port:
              number: 80
